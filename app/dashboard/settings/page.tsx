import React from 'react'

export default function DashboardSettingsPage() {
  return (
    <div>
      <h1>Dashboard Settings</h1>
      <p>Settings nested under the dashboard layout — the sidebar remains visible.</p>
    </div>
  )
}
